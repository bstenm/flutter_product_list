import 'package:flutter/material.dart';

class AdminProductCreate extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Create A New Product'),
    );
  }
}